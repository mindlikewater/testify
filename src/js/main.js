function fizz (number) {
  if (number % 3 === 0) {
    return "Fizz";
  } else {
    return number;
  }
};

function fizzBuzz (start, end) {
  // Write fizzBuzz here once fizz is finished.
};

export { fizz };
